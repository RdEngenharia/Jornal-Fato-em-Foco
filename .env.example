# Copie este arquivo para .env.local e preencha com seus valores reais.
# NUNCA faça commit do .env.local — ele já está no .gitignore.

# Obtido automaticamente ao criar um banco Postgres no dashboard da Vercel
# (Storage -> Create Database -> Postgres) e conectar ao projeto.
POSTGRES_URL=""

# Obtido em https://console.groq.com (gratuito, sem cartão de crédito)
GROQ_API_KEY=""
